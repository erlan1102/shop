import React from 'react';

const Form = () => {
    return (
            <div className='form'>
                <input type="search"/>
                <button type='button'>Поиск</button>
            </div>
    );
};

export default Form;